<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Marvel</title>
    <link href='https://fonts.googleapis.com/css?family=Open+Sans:300' rel='stylesheet' type='text/css'>
  </head>
  
  <body>
    <div class="container">
      <fieldset>
        <form action="/" method="post">
          <input name="hero" type="text" class="ghost-input" placeholder="Enter a Superhero" required>
          <input type="submit" class="ghost-button" value="Get Superhero">
        </form>
        <% if(hero !== null){ %>
          <p><%= hero %></p>
        <% } %>

        <% if(error !== null){ %>
          <p><%= error %></p>
        <% } %>
      </fieldset>
    </div>
    
  </body>
</html>